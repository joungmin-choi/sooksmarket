<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>숙명인 인증</title>
    <% include ./sm_head %>
    <script>
    var authenticationCode = <%= userCode %>;
    var isExisted = <%= isExisted %>;

    window.onload = function(){
      if(isExisted == 0){
        alert("해당 숙명 웹메일로 인증번호가 전송되었습니다.\n웹메일에서 받으신 인증번호를 밑에 적어주세요.");
      }else{
        alert("이미 등록된 이메일입니다.\n\n다른 숙명 웹메일 아이디를 입력하세요.");
        window.close();
        opener.checkingSookmyung = 0;
        opener.form.email.value="";
        opener.form.email.focus();
      }
    }
    function authenticate(){
      var codeText = document.getElementById("code");
      if(codeText.value==""){
        alert("인증번호를 입력해주세요!");
      }
      else{
        if(codeText.value == authenticationCode){
          alert("인증되었습니다!");
          window.close();
          opener.checkingSookmyung = 1;
          opener.form.phone.focus();
        }
        else{
          alert("인증번호가 다릅니다.\n다시 입력하세요.");
        }
      }
    }
    </script>
  </head>
  <body>
  <div class="container">
    <div class="row text-center">
      <div class="form-group">
      <div class="col-sm-5">
    <h3>숙명인 인증</h3><br/>
    인증번호 :<input type="text" class="form-control" name="code" id ="code" style="width:30%;display:inline;"/>
    <input type="button" class="btn btn-primary" value="인증하기" onclick="authenticate();" />
  </div>
</div>
  </div>
  </div>
  </body>
</html>
